# Anomaly Detection System
Python-based Z-Score anomaly detection system for network traffic. Reads a labeled CSV dataset, computes anomaly scores and Z-Scores, flags outliers, and reports TP/FP/FN, precision, recall, and F1. Includes both a CLI tool and a simple Flask web interface
